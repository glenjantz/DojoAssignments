var arr = ["James","Jill","Jane","Jack"];  // declares an array with 4 strings
for(var i=0;i<arr.length;i++){             //starts a loop where i is the variable and if its lets than the array length runs the code
  console.log(arr[i]);                     //logs the current index of i
}
